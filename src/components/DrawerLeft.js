import React from 'react';

const DrawerLeft = (props) => {
  // <button onClick={() => props.createRecipe({name: "TestRecipe", user_id: 1})}>CREATERECIPE</button>
  return <div className='drawer-left'>Drawer Left</div>;
};

export default DrawerLeft;